spring.config.import=configserver:${CONFIG_SERVER_URL:http://127.0.0.1:7012}
spring.cloud.config.name=users


spring.cloud.config.username=${CONFIG_SERVER_USERNAME:client}
spring.cloud.config.password=${CONFIG_SERVER_PASSWORD:RopheeBaby}
spring.cloud.config.fail-fast=false

#chck the username and password, might cause issues

